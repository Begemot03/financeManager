import { defineStore } from "pinia";
import { reactive, ref } from "vue";

export const useNewTaskModal = defineStore("newTaskModal", () => {
    const newDeposite = reactive({

    });

    const isVisible = ref(false);

    function closeModal() {

    }

    function openModal() {

    }

    
});